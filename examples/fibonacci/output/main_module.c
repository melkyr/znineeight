/* Generated by RetroZig bootstrap compiler */
#include "zig_runtime.h"

#include "main.h"
#include "std.h"

static unsigned int fib(unsigned int n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

int main(int argc, char* argv[]) {
    unsigned int result;
    result = fib(10);
    z_std_debug_printInt(__bootstrap_i32_from_u32(result));
}
