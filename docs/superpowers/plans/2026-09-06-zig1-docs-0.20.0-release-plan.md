# Zig1-Era Docs + Z98 0.20.0 "Oxalic Acid" Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the self-hosted (zig1) era the visible front of the repo documentation, add the Z98 0.20.0 "Oxalic Acid" release notes/timeline, and produce (a) the fresh RAM/wall-clock measurements (incl. the gcc ~80 MB caveat), (b) the PII/PIII estimates, and (c) a staged release artifact set (zig0/zig1 linux32 + win32 binaries, generated-C + source archives) — with zero compiler-source changes.

**Architecture:** Pure docs + measurement + artifact-staging plan. Facts (timeline, machine, gates, fixed point) are captured verbatim from verified sources; measurements and artifacts are produced by execution tasks and cross-checked; all numeric claims in docs are read from the evidence, never invented. Artifacts live under git-ignored `release/staging/`.

**Tech Stack:** build machine (AMD Ryzen 7 7700X, 32 GB, Debian), `g++/gcc 12.2.0`, `i686-w64-mingw32-gcc 12-win32`, `sf/build/zig0` + reference `/tmp/fx_subfolder/zig1`, `/usr/bin/time -v`, `tar`, `unzip` (zip created via a plan-resolved tool), wine `/tmp/wine32` (optional smoke).

Design spec: `docs/superpowers/specs/2026-09-06-zig1-docs-0.20.0-release-design.md` (operator-approved).

## Global Constraints

- **ZERO compiler-source edits** (`sf/src`, `sf/src/include/*`, examples, goldens). The 4-MD5 gate values (gol `302df36b`, lisp `3591bad9`, json `76056b97`, mud `846106ac`), self-compile fixed point `10f0ca2b`, corpus counts, EXPECTED_FAIL v70, QUICK_REF records are **read-only authoritative sources** — docs may quote them, never alter them.
- Repo doc changes are limited to: `README.md`→`README_zig0_bootstrap.md` (verbatim rename), new `README.md`, `RELEASENOTES.md`, `CHANGELOG.md`, `.gitignore` (single added line `release/staging/`), plus the two plan/spec docs. Artifacts are NEVER committed.
- Timeline (verbatim): repo/zig0 start **2025-12-07**; bootstrap-era end / `zig1_start` born **2026-04-27** (first self-hosted commit `cb11d5c6` "Self hosting initial milestone0 commit", `main` last commit same day); today **2026-09-06**; **1500** commits on `zig1_start` since `main`. Elapsed: project ~9 months; bootstrap ~4.7 months; self-hosted/0.20.0 window 2026-04-27→2026-09-06 (~4.3 months).
- Build machine statement (verbatim): "AMD Ryzen 7 7700X 8-core/16-thread up to ~5.58 GHz, 32 GB RAM, Debian; gcc/g++ 12.2.0; i686-w64-mingw32-gcc 12-win32".
- Measurements: rows A–D (g++→zig0, zig0→zig1 dump, gcc compile+link of the emitted C89, zig1 `-s0` self-compile), 3× each, `/usr/bin/time -v`, median reported; the gcc row is the "~80 MB" caveat and applies to every `zig1_5`-style hop (byte-identical emission).
- Min-requirements text MUST state: running zig1 fits ~16 MB at `-s0`, but BUILDING the compiler from source is dominated by the gcc backend (~80 MB measured) — a 16 MB machine can run zig1 but cannot build it from source.
- PII/PIII estimates: clock-scaled table (PII ~300–450 MHz, PIII ~700–1000 MHz), explicit methodology + uncertainty multiplier 1.0 default, memory-bound phases 1.5–3×, flagged as estimates for the operator's real VM.
- Artifacts (8 files + MANIFEST.txt) staged under `release/staging/` (git-ignored). Names EXACT: `zig0_linux32`, `zig0_w32.exe`, `zig1_linux32`, `zig1_w32.exe`, `src_c_zig1_linux.tgz`, `src_c_zig1_w32.zip`, `zig1_src.tgz`, `zig1_src.zip`. `zig1_src.*` each contain the identical tree: `sf/src/*.zig` + `sf/src/include/` + `RELEASE_README.txt`. No `zig0` source archive.
- Win-facing archives are `.zip` (win9x-unpackable); linux-facing `.tgz`. `zig1_src.*` RELEASE_README.txt states: compile with zig0 — download `zig0_linux32` or `zig0_w32.exe` from this same release; then exact where/how commands.
- Standalone workflow: subagent-driven (fresh implementer + independent reviewer per task, reviews Approved); STOP-present on any divergence/blocker/plan-vs-evidence mismatch; fastedit per docs/sf/AGENTS.md X.7 for edits; stage ONLY intended files; pre-existing dirty set (2026-08-26 plan doc, `mnemoria/*`, `.zig1_*.tmp`, `build/`, `examples/z98/json_parser_upgraded/`) never staged; memory agent `release0200-session`; reports accumulate in `.superpowers/sdd/task-RELEASE0200-report.md` (gitignored); ledger `.superpowers/sdd/progress.md`.

---

### Task 1: Baseline facts + authoritative recipes (record-only, no commit)

**Files:**
- Record only: `.superpowers/sdd/task-RELEASE0200-report.md` header + baseline section.

**Interfaces:**
- Produces: (a) verified baseline fact table (HEAD commit sha; `sf/build/zig0` presence + md5; reference `/tmp/fx_subfolder/zig1` md5 must be `c8f1b3d0` with std lib in `/tmp/fx_subfolder/lib`; 4-MD5 gate values; fixed point `10f0ca2b`); (b) the authoritative build-recipe set (exact dump/gcc/link commands for zig0 linux32 + win32 and for zig1 from emitted C89, both linux `-m32` and win32 `-osw`/mingw) resolved from `docs/sf/QUICK_REF.md` (build/self-compile sections) + `sf/scripts/build_release.sh` + `scripts/self_compile/build_next_gen.sh`; (c) zip-creation tool resolution.

- [ ] **Step 1: Capture the fact table.** Run `git rev-parse --short HEAD`, `md5sum /tmp/fx_subfolder/zig1` (expect `c8f1b3d0…`; if it differs, STOP-present — do not proceed on a stale compiler), `ls /tmp/fx_subfolder/lib` (expect the 4 std .zig files), `md5sum sf/build/zig0`, and re-run the 4-MD5 gate command (repo-root CWD `timeout 120 /tmp/fx_subfolder/zig1 --dump-c89 examples/z98/{game_of_life,lisp_interpreter_curr,json_parser,mud_server}/main.zig | md5sum`) → record against the constraint values. Record the timeline + machine statements verbatim (Global Constraints). Record `file` info for the mingw toolchain and `python3 --version` (for zip creation) plus `tar --version`.

- [ ] **Step 2: Resolve the authoritative build recipes from `docs/sf/QUICK_REF.md`.** Read the QUICK_REF build/self-compile/cross sections (and `sf/scripts/build_release.sh`, `scripts/self_compile/build_next_gen.sh`) and record EXACT command sets for: (1) building `zig0` (C++98) for linux (`g++ -std=c++98 -Isrc/include src/bootstrap/bootstrap_all.cpp -o …`) and for win32 (`i686-w64-mingw32-g++ … -lwsock32`); (2) dumping `sf/src/main.zig` with `zig0` → C89 into a fresh dir (flags/`-I` for the module graph incl. its std module path); (3) gcc compile+link of that emitted C89 → `zig1` (runtime sources `sf/src/include/zig_runtime.c` + `zig_pal.c`; include path); (4) the win32 flavour of (2)+(3) (`-osw`, `net_prelude.h`, `-lwsock32`) and the `zig1 -s0 --dump-c89 …` self-compile form. Record where `zig1` finds its `std` lib (exe-relative `lib/` with the 4 std .zig files, or `-I`) and encode that into the artifact layout. If any recipe step is ambiguous or machine-specific (`/tmp/fx_subfolder` baked into a script), STOP-present with the captured script text rather than guessing.

- [ ] **Step 3: Resolve zip creation.** `zip` is not installed. Verify `python3 -m zipfile -c <out.zip> <dir>` works for building a zip (test in /tmp). Record the working command (primary); fallback `i686-w64-mingw32-gcc`-agnostic `tar`-to-zip is NOT available — python3 `zipfile` is the tool. Verify `unzip -t` on the produced zip. Record.

- [ ] **Step 4: Report + ledger.** Write the baseline + recipes into the report; ledger line. No commit, no source edits.

---

### Task 2: Measurement battery + PII/PIII estimates (record-only, no commit)

**Files:**
- Record only (report sections; results land in the docs via Task 4).

**Interfaces:**
- Consumes: Task 1 recipes.
- Produces: the 4-row measurement table (median of 3) + optional extras + the PII/PIII clock-scaled estimate table with methodology.

- [ ] **Step 1: Row A — g++ generating zig0.** In a fresh `/tmp` workdir run `/usr/bin/time -v g++ -std=c++98 -Isrc/include src/bootstrap/bootstrap_all.cpp -o <W>/zig0_a` (repo-root CWD; exact flags per Task-1 recipe). Capture wall ("Elapsed (wall clock)") and RSS ("Maximum resident set size (kbytes)"). Repeat 3× fresh (fresh output name each time to avoid incremental-link caching). Record median.
- If `-m32` is part of the resolved linux recipe, ALSO run the linux32 variant once and note it as an extra.

- [ ] **Step 2: Row B — zig0 generating zig1's C89.** `/usr/bin/time -v <W>/zig0_a --dump-c89 --output-dir <fresh> sf/src/main.zig` (per Task-1 recipe, module search flags as resolved). Expect 0 `error[`, 0 PANIC. 3×; median wall + RSS recorded. (The emitted-C count is also recorded here — it feeds the archive step.)

- [ ] **Step 3: Row C — gcc compiling + linking the emitted C89 (the ~80 MB row).** In the fresh output dir from Step 2, compile each emitted `.c` → `.o` with `gcc -m32 -std=c89 -Wno-long-long -Wno-pointer-sign -I /workspace/znineeight/sf/src/include` and link `.o` + `sf/src/include/zig_runtime.c` + `zig_pal.c` into the zig1 binary, all under `/usr/bin/time -v` so the peak RSS across the whole gcc phase is captured (record the max gcc RSS observed and total wall). 3×. This row applies to every `zig1_5`-style hop (byte-identical emission), stated in the report. Optional extra: repeat the gcc compile once with the win32 flavour (`i686-w64-mingw32-gcc -osw …`) and record its RSS.

- [ ] **Step 4: Row D — zig1 `-s0` self-compile.** `/usr/bin/time -v zig1 -s0 --dump-c89 --output-dir <fresh> sf/src/main.zig` using the binary built in Step 3. 3×; median. Also record the `pool=` figure from `--markers --track-memory` once (`-s0` ≈ 15 MB pool) as supporting data for the "~16 MB at -s0" claim.

- [ ] **Step 5: PII/PIII estimates.** Build the clock-scaled estimate table from the row medians: for PII representative 300–450 MHz and PIII 700–1000 MHz, `T_p = T_m × f_m / f_p × k`, `k=1.0` default; present a range (k=1.0 lower bound; memory-bound caveat 1.5–3× as the stated upper rationale). Use `f_m` = 4.0 GHz as the conservative single-core figure of the 7700X for the table. Record methodology + caveat ("engineering estimate; verify on the operator's real VM") in the report verbatim for doc reuse.

- [ ] **Step 6: Report + ledger.** All tables + evidence into the report. No commit.

---

### Task 3: Staged release artifacts

**Files:**
- Modify: `.gitignore` (append one line `release/staging/`)
- Create (NOT committed): `release/staging/*` artifacts per Global Constraints manifest
- Record: report + ledger.

**Interfaces:**
- Consumes: Task 1 recipes + Task 2 emitted-C baseline.
- Produces: the 8 artifacts + `MANIFEST.txt` under git-ignored `release/staging/`.

- [ ] **Step 1: `.gitignore` + staging dir.** Append `release/staging/` to `.gitignore` (fastedit, X.7: re-read tail first; exact one-line addition). `mkdir -p release/staging`. Verify `git check-ignore release/staging` matches.

- [ ] **Step 2: Build `zig0_linux32` and `zig0_w32.exe`.** Per Task-1 recipe: linux via `g++ -std=c++98 -m32 …` (record flags; if the multilib `-m32` link fails with missing 32-bit libs, STOP-present with the exact error — do NOT silently build a 64-bit zig0), win32 via `i686-w64-mingw32-g++ … -lwsock32`. Smoke each: `zig0_linux32` compiles an `examples/z98/hello`-class program (dump rc 0); `zig0_w32.exe` is a PE32 (`file`), and if wine is available run it on a hello dump (rc 0). Record md5s.

- [ ] **Step 3: Build `zig1_linux32` and `zig1_w32.exe`.** Per Task-1 recipe: emit `sf/src/main.zig` C89 (gen-0 via the Task-2 Row-B output is acceptable; use the reference zig1 for the dump), then gcc `-m32` (linux) and `i686-w64-mingw32-gcc` with the win32 flavour (win) → binaries into staging. Each binary must find its `std` lib: create `release/staging/lib/`? NO — the staged binaries must be self-contained per release layout: ship each binary WITH the 4 std .zig files in a sibling `lib/` under staging (e.g., `release/staging/zig1_linux32` + `release/staging/lib/`); record the resolved relative-lib rule. Smoke: `zig1_linux32 --dump-c89 examples/z98/hello/main.zig` rc 0 into a fresh dir (0 `error[`), and a wine smoke of `zig1_w32.exe` (dump rc 0). Record md5s (note: linux32 and win32 binaries will differ — expected).

- [ ] **Step 4: Generated-C archives.** Fresh dirs: `(cd /workspace/znineeight && <zig1> --dump-c89 --output-dir <linuxW> sf/src/main.zig)` (linux flavour) and `… -osw --output-dir <winW> sf/src/main.zig` (win flavour). Exclude scratch `.zig1_*.tmp`. Pack: `tar -czf release/staging/src_c_zig1_linux.tgz -C <linuxW> .` and `python3 -m zipfile -c release/staging/src_c_zig1_w32.zip <winW>/` per Task-1 zip recipe. Verify integrity (`tar -tzf` lists expected .c/.h; `unzip -t` clean) and record file counts.

- [ ] **Step 5: `zig1_src` archives + RELEASE_README.txt.** Assemble a staging tree `release/staging/zig1_src_tree/` containing: `sf/src/*.zig` (recursive copy of the .zig module sources incl. the std module files the compiler needs), `sf/src/include/` headers, and a `RELEASE_README.txt` authored verbatim-ish: "Z98 0.20.0 'Oxalic Acid' — zig1 self-hosted compiler source. This source is NOT directly compilable by a C toolchain; it must first be compiled with the zig0 bootstrap compiler. Download `zig0_linux32` or `zig0_w32.exe` from this same release, then: [exact where/how — the resolved Task-1 commands: dump `sf/src/main.zig` with zig0, gcc/mingw compile+link of the emitted C with the include/runtime set, place the 4 std .zig files in the `lib/` next to the produced binary]." Pack into `zig1_src.tgz` and `zig1_src.zip` (identical content), verify both unpack (`tar -tzf`; `unzip -t`). Record checksums + file counts.

- [ ] **Step 6: MANIFEST.txt.** md5 + sha256 + size for every file in `release/staging/` (the 8 artifacts + the two archive format pair contents), build-machine line (verbatim), HEAD commit sha, date. Write `release/staging/MANIFEST.txt`.

- [ ] **Step 7: Commit ONLY the `.gitignore` line.**

```bash
git add .gitignore
git commit -m "chore: git-ignore release/staging artifact dir"
```

- [ ] **Step 8: Report + ledger.** Full manifest table + smokes + STOP-notes (any recipe divergence recorded). 

---

### Task 4: README restructure (bootstrap rename + new self-hosted README)

**Files:**
- Rename: `README.md` → `README_zig0_bootstrap.md` (verbatim — `git mv`, zero content change)
- Create: `README.md` (new, per spec §8.1)
- Record: report + ledger.

**Interfaces:**
- Consumes: spec §3/§4/§5/§6 fact+measurement content (Task 1 + Task 2 report), spec §8.1 outline.
- Produces: the repo's new front-page self-hosted README; bootstrap README preserved byte-identical under the new name.

- [ ] **Step 1: Rename with git.** `git mv README.md README_zig0_bootstrap.md`. Verify `git status` shows a pure rename (no content diff). At the very top of `README_zig0_bootstrap.md` do NOT edit content — instead (if a pointer is wanted) add nothing to the file itself: the new README carries the cross-pointer (spec §8.1.3). Keep the rename pure.

- [ ] **Step 2: Author the new `README.md`.** Full markdown per spec §8.1 sections 1–17. Rules: every number is taken verbatim from (a) this plan's Global Constraints timeline/machine text, (b) the Task-2 report measurement/estimate tables, (c) re-verified gate values (run the 4-MD5 gate once in this task and paste the exact md5 prefixes; paste fixed point `10f0ca2b`, EXPECTED_FAIL v70, corpus summary read from QUICK_REF/EXPECTED_FAIL). Content conventions: title `# Z98 0.20.0 "Oxalic Acid" — Self-Hosted Compiler (zig1)`; a short banner line pointing bootstrap-era users to `README_zig0_bootstrap.md`; the timeline section (dates + elapsed verbatim); determinism section (fixed point + 4-program gate table + self-emission byte-identity sentence); the A–D measurement table with the gcc ~80 MB caveat callout; min-requirements table (RUN vs BUILD; linux/windows) explicitly noting "zig1 runs in ~16 MB at -s0, but building the compiler from source needs the gcc backend ~80 MB"; PII/PIII estimate table + methodology; build-from-source recipe summary pointing at the release `zig1_src` archives + `README_zig0_bootstrap.md` for the bootstrap toolchain. Any number you cannot verify is left OUT (never invented) and noted in the report for a follow-up.

- [ ] **Step 3: Self-review the README** against spec §8.1: all 17 sections present, all numbers sourced, no stale bootstrap-era claims presented as current (e.g., zig0 flags belong to the bootstrap README; the new README's flag list is the zig1 set). Fix inline.

- [ ] **Step 4: Commit.**

```bash
git add README_zig0_bootstrap.md README.md
git commit -m "docs: Z98 0.20.0 Oxalic Acid self-hosted README (bootstrap README preserved as README_zig0_bootstrap.md)"
```

- [ ] **Step 5: Report + ledger.**

---

### Task 5: RELEASENOTES.md + CHANGELOG.md top entries

**Files:**
- Modify: `RELEASENOTES.md` (new top entry above the existing `# Z98 0.13.0 "2-Propanol"` section)
- Modify: `CHANGELOG.md` (new top `## [0.20.0] - "Oxalic Acid"` block above `[Unreleased]`/`[0.13.0]`)
- Record: report + ledger.

**Interfaces:**
- Consumes: spec §8.2/§8.3 outlines + timeline/machine (Global Constraints) + Task-2 tables + Task-3 manifest.
- Produces: release-record top entries; bootstrap entries untouched below.

- [ ] **Step 1: RELEASENOTES top entry.** Insert ABOVE line 1 (current `# Z98 0.13.0 "2-Propanol" Release Notes`), preserving the existing file verbatim below. Content (markdown, no emoji unless matching file style — the file uses emoji headers; MATCH the file's existing section conventions but the entry itself states facts plainly): `# Z98 0.20.0 "Oxalic Acid" Release Notes` — announcement (major: first self-hosted release; the self-hosted `zig1` compiler now builds itself deterministically); Timeline section (spec §3 verbatim incl. elapsed and where zig1 dev began: `zig1_start` branch, 2026-04-27); headline capabilities (determinism/fixed-point; language-wins builtins + case ranges + export + cross-module pub-var stores + clean diagnostics; `-osl`/`-osw`/`--target`; std_net extern rewrite + `createTcpClient`; upgraded showcase examples); Build machine + toolchain (spec §4); Artifacts (spec §7 manifest summary + note win-facing are .zip; min-requirements + the zig1-16MB/gcc-80MB caveat); Contributors (append the project's established contributors lines). Do NOT touch the 0.13.0 header or any content below.

- [ ] **Step 2: CHANGELOG top block.** Insert `## [0.20.0] - "Oxalic Acid"` above `[Unreleased]`/`[0.13.0]`, matching the file's Added/Changed/Removed/Fixed style, summarizing the self-hosted era (each bullet verifiable against git history/ledger). Leave all existing blocks untouched.

- [ ] **Step 3: Verify bootstrap sections byte-identical** below the insertion point (`git diff` shows pure additive hunks at top). 

- [ ] **Step 4: Commit.**

```bash
git add RELEASENOTES.md CHANGELOG.md
git commit -m "docs: Z98 0.20.0 Oxalic Acid release notes + changelog (self-hosted milestone)"
```

- [ ] **Step 5: Report + ledger.**

---

### Task 6: Final verification + STOP-present

**Files:**
- Record only.

**Interfaces:**
- Consumes: all prior tasks.
- Produces: the go/no-go evidence + STOP-present for the operator.

- [ ] **Step 1: Gate immutability.** Re-run the 4-MD5 gate (expect gol `302df36b`/lisp `3591bad9`/json `76056b97`/mud `846106ac`) and confirm the self-compile fixed point md5 `10f0ca2b` still reproduces (one hop1==hop2 dump round-trip). Any move → STOP-present (no silent re-baseline).

- [ ] **Step 2: Tree review.** `git status`/`git log` — working tree carries ONLY: pre-existing dirty set + `.gitignore` change + the rename/new README + RELEASENOTES/CHANGELOG + plan/spec docs. No `sf/src`, examples, or golden diffs anywhere in the range.

- [ ] **Step 3: Artifact + archive integrity.** Re-verify the staged artifact set: 8 files present with MANIFEST md5s matching; `zig1_src` determinism PROOF: unpack `zig1_src.tgz`, build the compiler from source with the staged `zig0_linux32` (Task-1 recipe) → produced binary self-dump (`-s0 --dump-c89 sf/src/main.zig`) emits md5s byte-identical to `src_c_zig1_linux.tgz` contents → the release chain is internally consistent (record evidence; if the chain no longer reproduces, STOP-present with the evidence).

- [ ] **Step 4: Docs numeric audit.** Grep the new README/RELEASENOTES for every number against the evidence report (measurements, estimates, timeline, md5s) — zero invented values; report any discrepancy to STOP.

- [ ] **Step 5: STOP-present.** Full verdict: docs set, measurement tables, estimate table, artifact manifest, gate-immutability + determinism-proof evidence, tree hygiene. No further action until the operator directs (attach artifacts, adjust docs, final whole-branch review).

---

## Plan Self-Review

1. **Spec coverage:** rename+new README (Task 4), release/changelog top entries (Task 5), timeline+elapsed (both, from Global Constraints text), build-machine statement (Tasks 1/4/5), measurements A–D + gcc caveat + min-requirements (Tasks 1/2/4/5), PII/PIII estimates (Task 2), artifacts + archives + zig1_src RELEASE_README + no-zig0-src + zip/tgz + staging git-ignore + manifest (Task 3), verification + STOP (Task 6), out-of-scope respected (Global Constraints). Success criteria 1–6 map to Tasks 4/5, 1/2, 5, 2, 3, 6.
2. **Placeholder scan:** no TBD; numbers that only execution can produce are marked "record/verify then embed verbatim" with explicit non-invention rules.
3. **Type/name consistency:** artifact filenames match the spec manifest exactly and are repeated identically in Tasks 3/6; report file `task-RELEASE0200-report.md`; memory agent `release0200-session`.
